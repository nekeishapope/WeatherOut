name: weather_events_app
description: Weather-based local event discovery

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'


dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.2
  geolocator: ^12.0.0
  permission_handler: ^11.3.1
  cached_network_image: ^3.3.1
  url_launcher: ^6.3.0
  intl: ^0.20.2
  flutter_dotenv: ^5.1.0
  device_preview: ^1.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env

    